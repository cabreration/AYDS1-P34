import { SignupPage } from './signup.po'
import { browser } from 'protractor';

describe('vista de registro', () => {
    let page: SignupPage;

    beforeEach(() => {
        page = new SignupPage();
    });
});